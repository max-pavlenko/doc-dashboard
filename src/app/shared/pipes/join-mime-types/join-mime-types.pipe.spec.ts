import { JoinMimeTypesPipe } from './join-mime-types.pipe';

describe('JoinMimeTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinMimeTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
